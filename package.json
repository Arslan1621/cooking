{
  "name": "rest-express",
  "version": "1.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev-server": "NODE_ENV=development tsx server/index.ts",
    "build-server": "vite build && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "start": "NODE_ENV=production node dist/index.js",
    "check": "tsc",
    "db:push": "drizzle-kit push"
  },
  "dependencies": {
    "@clerk/clerk-react": "^5.56.2",
    "@clerk/express": "^1.7.52",
    "@google/generative-ai": "^0.24.1",
    "@hookform/resolvers": "^3.10.0",
    ...
  },
  "devDependencies": {
    "@replit/vite-plugin-cartographer": "^0.2.8",
    ...
  },
  "optionalDependencies": {
    "bufferutil": "^4.0.8"
  }
}
